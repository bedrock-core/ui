---
'@bedrock-core/ui-runtime': minor
---

**Breaking.** The form protocol is `v0009`, and its header is `corev0009`. The payload layout is unchanged, but a render pack from an earlier release no longer recognises these forms, so update the pack with the library.
