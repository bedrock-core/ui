---
'@bedrock-core/ore-styled': patch
---

Fix: the `transparent` Button variant now uses its own hover and pressed textures instead of reusing the default one for every state, so it visibly reacts to input.
