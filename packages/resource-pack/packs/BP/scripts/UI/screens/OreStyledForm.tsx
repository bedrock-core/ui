import { Panel, Text, type JSX } from '@bedrock-core/ui';

/** Placeholder — the ore-styled native `<Form>` demo lands here next. */
export function OreStyledForm(): JSX.Element {
  return (
    <Panel flexDirection={'column'} gap={4} padding={8}>
      <Text>{'§7Ore-Styled Form — coming soon.'}</Text>
    </Panel>
  );
}
