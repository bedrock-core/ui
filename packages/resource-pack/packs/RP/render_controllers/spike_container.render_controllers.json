{
	"format_version": "1.10.0",
	"render_controllers": {
		// Minimal pass-through so the spike entity does not depend on any vanilla
		// render controller (those carry mob-specific Molang we would inherit).
		"controller.render.bcui_spike_container": {
			"geometry": "Geometry.default",
			"materials": [
				{
					"*": "Material.default"
				}
			],
			"textures": [
				"Texture.default"
			]
		}
	}
}
