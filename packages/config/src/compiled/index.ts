// The screen lives in a `.screen.tsx` file: that suffix is what the build's
// conditional sugar rewrites, and the rows' fields are shown by conditionals.
export * from './scope.screen';
export { default } from './scope.screen';
