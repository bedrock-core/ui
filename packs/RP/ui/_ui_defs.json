{
	"ui_defs": [
		"ui/server_form.json",
		// Base control, all components should extend this
		"ui/core-ui/common/control.json", // [0,511] 512 bytes
		// Special type, we use in the TS the label as the base for the
		// "client" components as label is the simplest component where
		// we can pass data
		"ui/core-ui/common/router.json",
		// -----------------------------------------------
		// After the initial reserved bytes for the core, rest of bytes 
		// after 512 for its own use and 
		// available in #serialized_data binding
		// ----------------- Server only -----------------
		// "ui/core-ui/components/input.json"
		// "ui/core-ui/components/slider.json", 
		// "ui/core-ui/components/toggle.json" 
		// --------------- Client + Server ---------------
		"ui/core-ui/components/panel.json",
		"ui/core-ui/components/text.json"
	]
}